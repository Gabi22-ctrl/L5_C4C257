#ifndef JSON_H
#define JSON_H //evita que el archivo se incluya más de una vez en el mismo archivo fuente
//Declaro la función que será definida en json.c
void crear_json(const char *archivo_salida); //genera un archivo JSON y lo guarda en la ruta especificada

#endif
